# ============================================================
# MoheBatch 외부 서버용 환경 설정
# ============================================================
#
# 이 파일을 .env로 복사하여 사용하세요:
#   cp .env.external.example .env
#
# 실행:
#   docker compose -f docker-compose.external.yml up --build
#
# ============================================================

# ------------------------------------------------------------
# 메인 서버 IP (DB, Redis, ImageProcessor가 있는 서버)
# ------------------------------------------------------------
MAIN_SERVER_IP=100.99.236.50

# ------------------------------------------------------------
# OpenAI API (필수)
# ------------------------------------------------------------
OPENAI_API_KEY=sk-your-openai-api-key-here

# ------------------------------------------------------------
# Embedding Service (필수 - GPU 서버에서 실행)
# ------------------------------------------------------------
# 임베딩 서비스가 실행 중인 서버 IP와 포트
EMBEDDING_SERVICE_URL=http://YOUR_EMBEDDING_SERVER_IP:6000

# ------------------------------------------------------------
# Batch 설정 (선택 - 기본값 사용 가능)
# ------------------------------------------------------------
BATCH_TOTAL_WORKERS=10
BATCH_THREADS_PER_WORKER=1
BATCH_CHUNK_SIZE=2
BATCH_EMBEDDING_CHUNK_SIZE=5

# ------------------------------------------------------------
# Image Storage Path (호스트 경로)
# ------------------------------------------------------------
# 이미지를 저장할 로컬 경로
IMAGE_STORAGE_PATH=./images
